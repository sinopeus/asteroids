entity e;
double a;
double b;
double r;

while (true) {
	print self;
	foreach(ship, e){
		a := (((gety e) - (gety self)) / ((getx e) - (getx self)));
		b := (getdirection);
		r := (getradius e);
		while ((a - b) >= r){
			turn 0.1;
		}
		print a;
		print b;
		skip;
	}
}
